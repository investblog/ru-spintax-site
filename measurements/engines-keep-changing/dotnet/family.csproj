<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
  <ItemGroup>
    <!-- The version this article's table reports; raise it to score a newer one. -->
    <PackageReference Include="Spintax.Core" Version="0.1.3" />
  </ItemGroup>
</Project>
